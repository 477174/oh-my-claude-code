Delegate the following task to the `metis` agent (model: opus, pre-planning consultant — analyzes scope, prevents creep, validates acceptance criteria). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS