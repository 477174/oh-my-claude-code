Delegate the following task to the `sisyphus-junior` agent (model: sonnet, focused task executor — do-first philosophy, implements autonomously with scope discipline). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS