Delegate the following task to the `atlas` agent (model: sonnet, plan executor — coordinates multi-task execution, delegates to sisyphus-junior per task, never writes code directly). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS