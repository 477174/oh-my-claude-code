Delegate the following task to the `librarian` agent (model: opus, external docs/code search — finds library docs, API references, OSS implementations with citations). The user's request is:

$ARGUMENTS