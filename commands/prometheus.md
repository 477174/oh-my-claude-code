Delegate the following task to the `prometheus` agent (model: opus, strategic planner — never writes code, only plans). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS