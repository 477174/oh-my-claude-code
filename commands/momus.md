Delegate the following task to the `momus` agent (model: sonnet, plan reviewer — blocker-finder, not perfectionist, checks reference validity and task executability). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS