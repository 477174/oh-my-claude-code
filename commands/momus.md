Delegate the following task to the `momus` agent (model: opus, plan reviewer — blocker-finder, not perfectionist, checks reference validity and task executability). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS