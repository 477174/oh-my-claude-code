Delegate the following task to the `hephaestus` agent (model: opus, deep autonomous worker — end-to-end execution of complex multi-file tasks). Use the 6-section delegation prompt template. The user's request is:

$ARGUMENTS